#include "main.h"
#include <stdio.h>

/**
 * isupper - function that checks for uppercase character
 *
 * @c: checks iput of a function
 *
 * Return: 1 if 'c' is uppercase otherwise always 0
 */

int _isupper(int c)
{
	if (c >= 64 && c <= 90)
	{
		return (1);
		return (0);
	}
}
